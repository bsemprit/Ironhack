<!-- app/views/contacts/new.html.erb -->
<h1>Add a new contact</h1>
<% if !@contact.errors.full_messages.empty? %>
  <% @contact.errors.full_messages.each do |error|%>
    <%= error%>
  <% end%>
<%end%>

<form action="/contacts" method="POST">
  <div>
    <label for="contact_name">Name</label>
    <input type="text" name="contact[name]" id="contact_name" required>
  </div>

  <div>
    <label for="contact_address">Address</label>
    <textarea name="contact[address]" id="contact_address" required></textarea>
  </div>

  <div>
    <label for="contact_phone">Phone number</label>
    <input type="tel" name="contact[phone_number]" id="contact_phone" required maxlength="13">
  </div>

  <div>
    <label for="contact_email">Email</label>
    <input type="email" name="contact[email]" id="contact_email" required>
  </div>
  <button type="submit">Add this contact</button>
  <input type="hidden" name="authenticity_token"
      value="<%= form_authenticity_token %>">
</form>